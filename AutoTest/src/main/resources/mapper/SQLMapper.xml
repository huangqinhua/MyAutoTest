<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.huang.model.Token">
    <select id="findOpendId" parameterType="String" resultType="com.huang.model.Token">
        select * from openid
    </select>

    <select id ="findTopValue" parameterType="String" resultType="com.huang.model.Top">
        select * from top
    </select>
</mapper>

